# Horcrux Environment — 프로젝트 전략 문서

> 이 파일은 개발 방향의 일관성을 유지하기 위한 중심 문서입니다.
> 변경 이력은 `CLAUDE_CHANGES.md`, 사용법은 `README.md`를 참조하세요.

---

## 1. 프로젝트 정체성 (Why This Project)

**Horcrux Environment**는 DGIST BRM 연구실에서 제작한 14-DOF 뱀 로봇(Horcrux v2)의 강화학습 환경을 **Gymnasium 표준 패키지**로 배포하기 위해 작성된 프로젝트다.

### 핵심 목적

| 목적 | 설명 |
|------|------|
| **재현성** | 동일한 로봇 정의를 누구나 `pip install`로 불러올 수 있게 한다 |
| **이식성** | RL 알고리즘 코드와 환경 코드를 분리해 실험 관리를 단순화한다 |
| **Sim-to-Real** | MuJoCo 시뮬레이션에서 학습한 정책을 실제 로봇에 적용하는 것을 최종 목표로 한다 |

### 왜 패키지 형태인가

뱀 로봇 제어 연구는 환경 정의(XML, 보상함수, 관측 공간)가 실험마다 달라지기 쉽다. 이를 버전 관리된 패키지로 고정하면:
- 실험 재현이 가능하고
- 알고리즘 연구자와 로봇 연구자가 역할을 분리할 수 있으며
- 향후 다른 연구자가 동일 플랫폼 위에서 실험을 이어갈 수 있다.

---

## 2. 현재 구현 상태 (v0.0.1)

### 완성된 것

- **물리 시뮬레이션**: MuJoCo 기반 14-DOF 뱀 로봇 (`horcrux_p.xml`)
- **보행 패턴 생성기**: Hirose(1993) Serpenoid 곡선 기반 `GaitV2`
- **Gymnasium 환경**: `PlaneJoyDirWorld` (`horcrux_env/plane-v0`)
  - 관측 공간: 97차원 (관절 상태 42 + 접촉 28 + IMU 10 + 동작벡터 14 + 조이스틱 3)
  - 행동 공간: 14차원 연속 (토크 크기, [0, 2.7] Nm)
  - 보상: 선형이동 + 회전 + 효율 + 건강 상태 (다중 목적)
- **환경 변형**: 평지 / 장애물 / 모래 / 잔디 / PVC 플랫폼 (XML 변형)
- **래퍼**: ClipReward, DiscreteActions, RelativePosition, ReacherRewardWrapper
- **필터**: MovingAverageFilter (1D / 3D / Quaternion) — IMU 노이즈 완화용
- **마찰 무작위화**: 에피소드마다 지면 마찰 계수 랜덤 적용 가능

### 알려진 한계 및 미완성 요소

- `joy_input_random=True` 설정 시 실제로는 전방(1,0,0) 방향만 활성화됨 (8방향 코드 주석 처리 상태)
- `RelativePosition`, `ReacherRewardWrapper` 래퍼는 현재 환경 관측 형식과 미호환
- 노이즈 주입 기능(noise_low/high) 구현은 있으나 비활성화 상태
- 단일 환경 ID만 등록됨 (`plane-v0`); 지형 변형은 XML 수동 교체 필요

---

## 3. 핵심 설계 원칙 (변경 시 반드시 고려)

이 원칙들은 프로젝트 전반에서 일관되게 유지되어야 한다.

### 3-1. Gymnasium 표준 준수
모든 환경은 `gymnasium.Env` 인터페이스를 따른다.
- `observation_space`, `action_space`는 명시적으로 정의
- `reset()` → `(obs, info)`, `step()` → `(obs, reward, terminated, truncated, info)` 반환
- 써드파티 RL 라이브러리(Stable-Baselines3, CleanRL 등)와 즉시 호환되어야 한다

### 3-2. 행동 공간 = 크기, Gait = 방향
행동(action)은 토크의 **크기**만 결정하고, 방향은 `GaitV2`의 motion_vector가 결정한다.
이 분리는 의도된 설계다: RL 에이전트가 "얼마나 세게"만 결정하고, "어느 방향으로"는 생물학적 보행 패턴이 담당한다.
이 구조를 바꾸려면 보상함수와 관측 공간 전체를 재검토해야 한다.

### 3-3. 변환 행렬 기반 속도 계산
속도는 차분(MuJoCo qvel)을 직접 쓰지 않고, 연속 두 스텝 간 4×4 변환행렬(T_before⁻¹ × T_after)로 계산한다.
이는 로봇 좌표계 기준의 상대적 이동을 정확히 추출하기 위해서다. 유지해야 한다.

### 3-4. 보상 가중치는 파라미터로 노출
보상 함수의 각 항(선형이동, 회전, 효율, 건강)의 가중치는 생성자 인수로 노출한다.
실험 재현성을 위해 하드코딩하지 않는다.

### 3-5. XML은 단일 진실 소스
로봇의 물리 특성(질량, 관절 범위, 마찰, 센서)은 XML 파일에서만 정의한다.
Python 코드에 물리 상수를 하드코딩하지 않는다.

---

## 4. 개발 이정표 (Roadmap)

### Phase 1 — 기반 완성 `[현재 진행 중]`

목표: 단일 환경에서 안정적인 학습이 가능한 수준의 패키지 완성

- [ ] 8방향 조이스틱 입력 무작위화 활성화 (`joy_input_random`)
- [ ] 노이즈 주입 기능 활성화 및 범위 결정
- [ ] 환경 변형(지형)을 별도 등록 ID로 노출 (`plane-v0`, `sand-v0`, `obstacle-v0` 등)
- [ ] `DiscreteActions` 래퍼를 현재 환경과 호환되도록 수정
- [ ] 단위 테스트 추가 (환경 `reset/step` 무결성, 관측 차원 검증)

### Phase 2 — 커리큘럼 학습 지원 `[단기]`

목표: 단계적 난이도 증가를 지원하는 환경 설계

- [ ] 커리큘럼 래퍼: 학습 진행도에 따라 지형/마찰/노이즈 난이도를 자동 조정
- [ ] 다방향 목표 추종 환경: 에피소드마다 목표 방향을 무작위화 (현재는 전방만)
- [ ] 장애물 회피 환경: 장애물과의 거리를 관측에 추가
- [ ] 보상 정규화 래퍼: 온라인 보상 스케일링으로 학습 안정성 향상

### Phase 3 — Sim-to-Real 전이 `[중기]`

목표: 시뮬레이션 학습 정책을 실제 로봇에 적용 가능하게 함

- [ ] Domain Randomization 강화: 링크 질량, 관절 댐핑, 지면 마찰 무작위화 범위 확대
- [ ] 실제 로봇 센서와 관측 공간 정렬: 실제 IMU, 관절 인코더의 노이즈 특성을 시뮬레이션에 반영
- [ ] 정책 배포 인터페이스: 학습된 정책을 실제 로봇 제어 루프에 연결하는 어댑터 작성
- [ ] 실제 로봇 데이터와 시뮬레이션 비교 검증 파이프라인

### Phase 4 — 패키지 성숙 `[장기]`

목표: 외부 연구자가 사용 가능한 수준의 완성도

- [ ] PyPI 배포 (`pip install horcrux-env`)
- [ ] 기학습 정책(Pretrained Policy) 제공: 전방 이동, 회전, 장애물 회피
- [ ] Hydra/Gin 기반 설정 관리 통합
- [ ] 문서화 사이트 (ReadTheDocs 또는 MkDocs)
- [ ] Benchmark: 주요 RL 알고리즘(PPO, SAC, TD3)의 기준 성능 기록

---

## 5. 열린 설계 질문 (결정이 필요한 사항)

이 항목들은 아직 방향이 정해지지 않았으며, 실험 결과나 연구 방향에 따라 결정된다.

| 질문 | 현재 상태 | 고려 사항 |
|------|----------|-----------|
| 관측 공간에 목표 방향을 포함할 것인가? | 포함 (joy_input 3D) | 제거하면 목적 비의존 정책 학습 가능 |
| 접촉 센서를 이진화할 것인가? | 이진화 (>1N) | 연속 힘 값이 더 많은 정보를 줄 수 있음 |
| Gait 파라미터를 에이전트가 직접 학습할 것인가? | 고정 (사람이 설정) | Meta-learning / 계층적 RL 가능성 |
| 다중 에이전트(여러 뱀 로봇) 환경이 필요한가? | 단일 에이전트 | 향후 군집 로봇 연구 방향 |
| Privileged Information을 활용한 Teacher-Student 학습? | 미적용 | Sim-to-Real 전이에 유효한 기법 |

---

## 6. 주요 파일 구조 참조

```
horcrux_env/
├── envs/
│   ├── plane_v3.py       # 메인 환경 (PlaneJoyDirWorld)
│   └── gait_v2.py        # Serpenoid 곡선 보행 패턴 생성기
├── wrappers/             # 환경 래퍼 모음
└── resources/
    ├── horcrux_p.xml     # 로봇 물리 모델 (단일 진실 소스)
    ├── horcrux_plane.xml # 기본 환경 (평지)
    └── assets/           # STL/OBJ 메시 파일
```

---

## 7. 용어 정의

| 용어 | 정의 |
|------|------|
| **Serpenoid 곡선** | Hirose(1993)가 제안한 뱀 보행의 수학적 모델. 등쪽(dorsal)과 측면(lateral) 정현파의 합으로 각 관절 궤적을 생성 |
| **Motion Vector** | Gait 패턴에서 추출한 각 관절 토크의 부호(+1/-1). 행동의 방향을 결정 |
| **joy_input** | 조이스틱 입력을 모사한 3D 목표 벡터 [x속도, y속도, yaw속도]. 에이전트가 추종해야 할 방향 |
| **건강 상태(healthy)** | 로봇의 roll 각도가 ±45° 이내인 상태. 벗어나면 페널티, ±120° 초과 시 에피소드 종료 |
| **frame_skip** | 하나의 env.step() 호출당 진행되는 물리 시뮬레이션 스텝 수 (기본 10, = 50ms/step) |
